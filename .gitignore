# Oversized archive (113MB+) — GitHub rejects files >100MB
# Contents already exist unpacked in gesture_models_export/
gesture_models_export.zip

# Large dataset archive (optional local-only; uncomment to exclude)
# two_hand_id_datasets.zip

# Python / OS
__pycache__/
*.pyc
.DS_Store
Thumbs.db
.env
*.log
